import PopUpHeader from ".";

export default{
    title: 'Atoms/PopUpHeader',
    component: PopUpHeader,
};

const Template = args => <PopUpHeader {...args} />

export const Default = Template.bind();